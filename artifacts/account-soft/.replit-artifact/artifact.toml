kind = "web"
previewPath = "/"
title = "AccountSoft"
version = "1.0.0"
id = "artifacts/account-soft"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25833

[services.development]
run = "pnpm --filter @workspace/account-soft run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/account-soft", "run", "build" ]
publicDir = "artifacts/account-soft/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25833"
BASE_PATH = "/"
